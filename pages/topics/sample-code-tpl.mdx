import { Tabs, Tab, Callout } from 'nextra-theme-docs'

### Hook    
    
```
do_action( 'jb-after-job-delete', $job_id, $post_data )
```

#### Description
    <div class="description">
        <p>Fires after Job has been deleted.</p>
    </div>
#### Parameters
    
| Name          | Type        |   Description |
| :------------ | :---------: | ----------: |
| $job_id       |    int      | Deleted job ID. |
| $post_data    |   WP_Post   | The deleted job's post object. |


#### Since
    1.1.0


#### Source
-  <a href="includes_ajax_class-jobs.php.html">includes/ajax/class-jobs.php</a>, <a href="includes_ajax_class-jobs.php.html#line786">line 786</a>

#### Usage Example

<Tabs items={['PHP', 'Javascript']}>
  <Tab>
    ```php
   add_action( 'jb-after-job-delete', 'um_jb_after_job_delete', 10, 2 );
   function um_jb_after_job_delete( $job_id, $post_data ){

       // Do something

   }
    ```
  </Tab>
   <Tab>
    ```js
    jQuery(document).on("ready",function(){
        // Do something
    });
    ```
  </Tab>
</Tabs>
