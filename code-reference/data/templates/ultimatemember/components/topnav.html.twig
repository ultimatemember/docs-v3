<nav className="phpdocumentor-topnav">
    {% for key,menu in topMenu|default([]) %}
        <ul className="phpdocumentor-topnav__menu -{{ key }}">
            {% for menuitem in menu %}
                <li className="phpdocumentor-topnav__menu-item">
                    <a href="{{ menuitem.url }}">
                        {% if menuitem.icon %}
                        <i className="fab fa-{{ menuitem.icon }}">{{ menuitem.icon }}</i>
                        {% endif %}
                        {{ menuitem.name }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    {% endfor %}
</nav>
