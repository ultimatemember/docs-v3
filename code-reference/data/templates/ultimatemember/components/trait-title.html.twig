<h2 className="phpdocumentor-content__title">
    {{ node.name }}
    {% if node.usedTraits is not empty %}
        <span className="phpdocumentor-trait__extends">
            Uses
            {% for trait in node.usedTraits %}
                {{ trait|route('url') }}{% if not loop.last %}, {% endif %}
            {% endfor %}
        </span>
    {% endif %}
</h2>
